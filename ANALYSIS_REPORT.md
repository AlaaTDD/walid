# تقرير التحليل العميق للمشروع — Walid Sheet Nesting

**تاريخ التقرير:** 2026-08-20 (نسخة محدَّثة ونهائية لهذه الجلسة)
**النطاق:** قراءة كاملة، سطراً بسطر، لكل ملف بلا استثناء في:

- `backend/app/**` (geometry, nesting, validation, rasterization, api, main.py, core_logging.py, image_safety.py) — **27 ملف Python**
- `web/src/**` (types, lib, components, app) — **20 ملف TypeScript/TSX**

**المنهجية:** لا تخمين. كل استنتاج هنا مبني على قراءة الكود الفعلي، وتتبع كل استدعاء ذي صلة (callers/callees، تدفق البيانات، الاختبارات الموجودة)، وتشغيل تجارب فعلية (scripts، pytest الكامل، grep شامل) عند الحاجة للتأكد من أن الفهم مطابق للسلوك الحقيقي — وليس على أسماء الملفات أو التعليقات وحدها. كل إصلاح تم التحقق من وجوده في الكود الحي مباشرة قبل كتابته هنا، وليس بالاعتماد على ذاكرة الجلسة فقط.

هذا التقرير **نسخة محدَّثة تحل محل** `backups/ANALYSIS_REPORT.md` (النسخة الأقدم، محفوظة كسجل تاريخي). كل ما ورد هناك ما زال صحيحاً وتم التحقق منه مجدداً هنا (القسم 1)، بالإضافة إلى تحليل جديد بالكامل لملفات لم تكن مقروءة وقتها (القسم 2 و3).

---

## الخلاصة التنفيذية

| #            | المشكلة                                                                                                                                       | الملف                                               | الخطورة                                    | الحالة                                                                 |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- | ------------------------------------------ | ---------------------------------------------------------------------- |
| ISSUE-1      | `simplify_tolerance_mm` بارامتر متجاهَل                                                                                                       | `geometry/contour.py`                               | متوسطة                                     | ✅ مُصلَح (من قبل)                                                     |
| ISSUE-2      | نمط `.index(part_id)` غير ضروري                                                                                                               | `nesting/engine.py`                                 | منخفضة                                     | ✅ مُصلَح (من قبل)                                                     |
| ISSUE-3      | نفس نمط ISSUE-2                                                                                                                               | `nesting/lns.py`                                    | منخفضة                                     | ✅ مُصلَح (من قبل)                                                     |
| ISSUE-4      | تنعيم مضاعف (double smoothing)                                                                                                                | `api/job_storage.py`                                | عالية                                      | ✅ مُصلَح (من قبل)                                                     |
| ISSUE-5      | كاش الـcollision يُكتب ولا يُقرأ                                                                                                              | `main.py`                                           | متوسطة                                     | ✅ مُصلَح (من قبل)                                                     |
| ISSUE-6      | الفرونت إند يرسم كل قطعة كمستطيل بدل الشكل الحقيقي                                                                                            | `nestingJobStore.ts` + الباكند                      | متوسطة                                     | ✅ مُصلَح ومؤكَّد (راجعته هذه الجلسة مباشرة عبر SheetLayoutCanvas.tsx) |
| **ISSUE-7**  | **عطل حرج: `run_nesting` يتعطل (ValueError) عند استخدام المسار السريع (fast candidate path)**                                                 | `nesting/engine.py`                                 | **حرجة (Blocker, 500 error)**              | ✅ **مُصلَح هذه الجلسة**                                               |
| **ISSUE-8**  | حذف قطعة لا يمسح كاش الـcollision المرتبط بها                                                                                                 | `main.py` (`delete_job_part`)                       | منخفضة (غير قابل للاستغلال حالياً)         | ✅ **مُصلَح هذه الجلسة** (دفاعي)                                       |
| **ISSUE-9**  | `ConfirmResponse` لا يرسل حقول `checked_*` الحقيقية من `QaReport`، فيضطر الفرونت إند لتخمين عدد أزواج الـclearance المفحوصة                   | `api/schemas.py` + `main.py` + `nestingJobStore.ts` | منخفضة (لا أثر حالي على المستخدم)          | 📋 **توصية موثقة (لم تُصلَح — راجع القسم 3)**                          |
| **ISSUE-10** | مؤشر "الاتصال بالباك إند المحلي" يفضل عالق على "غير متصل" حتى بعد ما السيرفر يشتغل فعلياً، لأن الفحص كان بيتنفذ مرة واحدة بس عند تحميل الصفحة | `web/src/app/page.tsx`                              | متوسطة (تجربة مستخدم مربكة، لا خلل بيانات) | ✅ **مُصلَح هذه الجلسة** (راجع القسم 5)                                |
| ملاحظة       | كود ميت غير مستخدم (2 حالة)                                                                                                                   | `tiff_export.py`, `nestingApi.ts`                   | لا خطورة                                   | 📋 **موثقة فقط (راجع القسم 4)**                                        |

**جودة الكود العامة:** المشروع، بعد هذا التحليل الكامل الثاني، ما زال يُظهر انضباطاً هندسياً عالياً جداً — لكن هذه الجلسة اكتشفت **عطلاً حرجاً حقيقياً وقابلاً للتكرار فعلياً (ISSUE-7)** كان كامناً منذ إضافة ميزة "Fine Rotation Refinement"، ولم يظهر في أي اختبار أو بيانات إنتاج حقيقية لأن كل الأشكال المخزَّنة حالياً (151 صورة ملصقات) أصغر من عتبة تفعيل هذا المسار. بالإضافة لذلك، اكتُشف وأُصلح لاحقاً في نفس الجلسة **ISSUE-10**: مشكلة واجهة مستخدم منفصلة تماماً (لا علاقة لها بمنطق الـnesting نفسه) حيث كان مؤشر "متصل/غير متصل" في شاشة الإعدادات يعرض حالة قديمة غير محدَّثة تلقائياً.

---

# القسم 1: المشاكل المؤكَّدة سابقاً (ISSUE-1 إلى ISSUE-6)

هذه الست مشاكل كانت موثَّقة في `backups/ANALYSIS_REPORT.md`، وتم **التحقق من كل واحدة منها مجدداً بالقراءة المباشرة للكود الحالي** في هذه الجلسة (وليس فقط نسخها من التقرير القديم). كلها لا تزال مُصلَحة وصحيحة.

## ISSUE-1: `simplify_tolerance_mm` كان بارامتر بلا أثر

**الملف:** `backend/app/geometry/contour.py`
كانت الدالة `_geometry_from_alpha` تتجاهل البارامتر الممرَّر وتستخدم قيمة ثابتة `2.5` دائماً. **الحل الحالي المؤكَّد:** الكود يستخدم `simplify_tolerance_mm` الممرَّر، أو يحسبه ديناميكياً `max(px_to_mm(0.5, resolution), 1.0)` إذا كان `None` — مع تعليق يشرح السبب صراحة (سطر 128-138 من الملف).

## ISSUE-2 و ISSUE-3: نمط `.index(part_id)` في `engine.py` و`lns.py`

**الملفات:** `nesting/engine.py`, `nesting/lns.py`
كان الكود يستخدم `list.index(part_id)` (بحث O(n)) بدل `idx` المتاح من `enumerate`. **الحل الحالي المؤكَّد:** كلا الملفين يستخدمان `order_index`/`loop_index` من الحلقة نفسها. تم التحقق عبر grep شامل: لا يوجد أي تطابق متبقٍ لهذا النمط في المشروع بالكامل.

## ISSUE-4: تنعيم مضاعف (Double Smoothing) في `job_storage.py`

**الملف:** `backend/app/api/job_storage.py`
كانت `part_inputs_from_state()` تُطبِّق `buffer(2.0).buffer(-4.0).buffer(2.0).simplify(1.0)` على شكل مبسَّط بالفعل من الرفع، مما يخالف مبدأ "الدقة الهندسية 100%" الموثَّق في المشروع. **الحل الحالي المؤكَّد:** الكود الحالي (سطر 390-402) يستخدم الشكل مباشرة من `contour_wkt` بدون أي تنعيم إضافي، مع تعليق موثَّق يشرح لماذا التنعيم الإضافي لم يكن ضرورياً.

## ISSUE-5: كاش الـCollision يُكتب ولا يُقرأ أبداً

**الملف:** `backend/app/main.py`
كانت `_validate_stored_sheets()` تستدعي `validate_layout()` بلا شرط رغم وجود كاش (`cached_collision_signature`) مخصَّص لهذا الغرض بالضبط. **الحل الحالي المؤكَّد:** الكود الحالي (سطر ~538-551) يفحص `_collision_signature(state)` أولاً ويعيد النتيجة المخزَّنة إذا تطابقت البصمة.

## ISSUE-6: الفرونت إند يرسم كل قطعة كمستطيل بدل الشكل الحقيقي

**الملفات:** `web/src/lib/nestingJobStore.ts`, `web/src/components/SheetLayoutCanvas.tsx`, `backend/app/api/schemas.py`, `backend/app/main.py`

**تم التحقق من هذه المشكلة بشكل مباشر ومستقل في هذه الجلسة** (وليس بالاعتماد على تقرير سابق فقط):

1. `schemas.py`'s `PlacedPartPreview` يحتوي فعلياً على `contour_mm: list[ContourPointPreview]`.
2. `main.py`'s `_exterior_ring_mm()` و`_placed_previews()` يستخرجان الحلقة الخارجية الحقيقية من `placed_shape_mm.exterior.coords`.
3. `nestingJobStore.ts`'s `parseContourMm()` (سطر 1205) و`decodePlaced()` (سطر 1216) يُفضِّلان الشكل الحقيقي (`realContour`) على المستطيل المصنَّع، مع الرجوع للمستطيل فقط في حالة عدم توفر `contour_mm` (توافق خلفي مع jobs قديمة).
4. **الأهم:** `SheetLayoutCanvas.tsx` (سطر 188-214) يرسم `part.contourMm` مباشرة كـ`Path2D` مضلع حقيقي — وليس مستطيلاً — مع اختبار hit-testing دقيق بخوارزمية point-in-polygon (سطر 55-100) بدل bounding box.

**الخلاصة: هذه المشكلة محلولة بالكامل عبر السلسلة الثلاث (schema → باكند → فرونت إند → الرسم الفعلي)، تم التحقق من كل حلقة فيها بالقراءة المباشرة.**

---

# القسم 2: مشاكل جديدة اكتُشفت وأُصلحت في هذه الجلسة

## ISSUE-7 (حرجة): عطل `run_nesting` عند استخدام المسار السريع (Fast Candidate Path)

**الملف:** `backend/app/nesting/engine.py`
**الخطورة:** **حرجة (Blocker)** — يُسبب HTTP 500 عند أول رفع لصورة أو شكل معقد بما فيه الكفاية

### المشكلة كما اكتُشفت

محرك الترتيب لديه مساران للـplacement:

- **المسار الدقيق (exact NFP path):** يستدعي `_place_one_part`، والتي تُرجع دائماً **4 قيم**: `(angle, final_shape, center, winning_rotation)` — لأنها تتضمن خطوة "Fine Rotation Refinement" الإضافية التي أُضيفت لاحقاً للمشروع.
- **المسار السريع (fast candidate path):** يُفعَّل تلقائياً فقط عندما يتجاوز إجمالي رؤوس المشروع `_FAST_PATH_TOTAL_VERTICES = 1,000,000` أو أي قطعة مفردة `_FAST_PATH_MAX_VERTICES = 100,000` رأس. يستدعي `_place_one_part_fast`، والتي تُرجع **3 قيم فقط**: `(angle, final_shape, center)` — لأنها لا تتضمن خطوة التحسين الدقيق للزاوية.

في الحلقة الرئيسية لـ`run_nesting` (حوالي السطر 1454-1466)، كان الكود يفكّ نتيجة أي من الدالتين بافتراض ثابت أنها دائماً 4 قيم:

```python
angle, final_shape, center, winning_rotation = result
```

**النتيجة:** عند تفعيل المسار السريع (`use_fast_candidate_path == True`) ونجاح أول عملية placement، يحدث:

```
ValueError: not enough values to unpack (expected 4, got 3)
```

مما يُسقط الطلب بالكامل بخطأ HTTP 500 عبر معالج main.py العام للاستثناءات، برسالة "فشل محرك الـnesting: not enough values to unpack...".

### التأكيد التجريبي

تم إعادة إنتاج المشكلة فعلياً (وليس نظرياً فقط) بكتابة سكربت اختبار بأشكال ذات رؤوس كثيرة تتجاوز العتبة، وتشغيله عبر `.venv/bin/python`:

1. **قبل الإصلاح:** السكربت يفشل فوراً بـ`ValueError: not enough values to unpack (expected 4, got 3)` عند أول قطعة توضع بنجاح على المسار السريع — تم تأكيد أن `use_fast_candidate_path=True` فعلياً عبر طباعة مباشرة.
2. **بعد الإصلاح:** نفس السكربت بالضبط ينجح تماماً: `placed=2/2`، بزوايا ومساحات صحيحة، بدون أي استثناء.
3. **إعادة تشغيل كل اختبارات pytest الموجودة** (كعملية خلفية مُدارة، 205 ثانية): **87 اختبار ناجح، 0 فاشل** — لا يوجد أي regression.

### لماذا لم يُكتشف من قبل

- **لا يوجد أي اختبار** في `backend/tests/` يغطي `use_fast_candidate_path == True` مباشرة (بحث grep شامل لم يُرجع أي نتيجة).
- **كل الـ17 job حقيقي مخزَّن** تحت `backend/jobs/` له contours مبسَّطة بالفعل عبر `geometry/contour.py`'s `simplify()` إلى ما دون عتبة المسار السريع بكثير.
- **حتى `benchmark.py`'s `generate_benchmark_parts`** ينتج دائماً مضلعات صغيرة (5-9 رؤوس لكل قطعة)، فيبقى `use_fast_candidate_path` دائماً `False` هناك أيضاً.

معنى ذلك: الخلل موجود منذ إضافة ميزة "Fine Rotation Refinement" (التي غيَّرت `_place_one_part` لترجع 4 قيم دائماً بدل 3)، لكنه لم يظهر أبداً في أي بيئة اختبار أو إنتاج حالية — وكان سيظهر فقط عند رفع صورة معقدة جداً بدقة عالية (مثل SVG معقد أو صورة بتفاصيل دقيقة لم تُبسَّط كفاية أثناء الاستخراج) أو job كبير جداً بمئات القطع عالية التفصيل.

### الحل المطبَّق

في الموضع الدقيق (`run_nesting`'s main loop)، تم إضافة تفريع صريح يعتمد على `use_fast_candidate_path` (المتاحة فعلياً في نفس النطاق):

```python
if use_fast_candidate_path:
    angle, final_shape, center = result
else:
    angle, final_shape, center, winning_rotation = result
```

(`occupied_zones.append(...)` يبقى محكوماً بـ`if not use_fast_candidate_path` كما كان تماماً — بدون تغيير).

### التحقق من عدم وجود نظائر أخرى لنفس الخلل

تم عمل **grep شامل** لكل استدعاءات `_place_one_part` و`_place_one_part_fast` في المشروع بالكامل. النتيجة: **4 مواضع استدعاء فقط بالمجموع**، وتم التحقق من كل واحد منها فردياً:

| الموضع                                     | الحالة                                                                          |
| ------------------------------------------ | ------------------------------------------------------------------------------- |
| `run_nesting`'s main loop                  | **كان معطوباً — تم إصلاحه الآن (ISSUE-7)**                                      |
| `_backfill_gaps` (سطر ~1285)               | سليم من البداية — يستدعي `_place_one_part` فقط دائماً (4 قيم دائماً، صحيح)      |
| المسار السريع's backfill sweep (سطر ~1594) | سليم من البداية — يستدعي `_place_one_part_fast` فقط دائماً (3 قيم دائماً، صحيح) |
| `lns.py`'s `_repair()`                     | سليم من البداية — يحتوي بالفعل على تفريع صحيح مطابق لنفس النمط المُصلَح الآن    |

**لا يوجد أي نظير آخر لهذا الخلل في المشروع.**

### الأثر على الإنتاج

تم تتبُّع سلسلة الاستدعاء كاملة: `compute_layout` (الـroute الأكثر استخداماً في الـAPI) → `run_best_single_sheet_nesting` → `run_nesting` (الدالة المُصلَحة). أي job يتجاوز عتبة المسار السريع كان سيتعطل بالضبط كما وُصف أعلاه، فور محاولة أول compute — وهذا يؤكد أن الخطورة الموصوفة (Blocker) لم تكن مبالغاً فيها.

---

## ISSUE-8: حذف قطعة لا يمسح كاش الـCollision المرتبط بها (دفاعي)

**الملف:** `backend/app/main.py` (الدالة `delete_job_part`)
**الخطورة:** منخفضة جداً — **غير قابل للاستغلال فعلياً في السلوك الحالي**، لكن تم إصلاحه للاتساق مع نمط موجود بالفعل في نفس الملف.

### المشكلة

`delete_job_part` (نقطة النهاية `DELETE /jobs/{job_id}/parts/{client_part_id}`) تُعيد ضبط 15 حقلاً عند حذف قطعة (لأن أي حذف قطعة يُبطل أي layout محسوب مسبقاً) — لكنها كانت تفوت أربعة حقول محددة: `cached_collision_signature`, `cached_collision_is_valid`, `cached_collision_violations`, `cached_collision_checked_pairs`.

هذه بالضبط نفس الحقول التي تُعيد `_upgrade_legacy_alpha_rejections` (مسار آخر في نفس الملف يُعيد ضبط الحالة لنفس السبب — تغيير بنيوي في `state.parts`) ضبطها بالفعل بشكل صحيح.

### لماذا هذا غير قابل للاستغلال حالياً

- القارئ الوحيد لهذه الحقول هو `_validate_stored_sheets` (المُصلَحة في ISSUE-5 أعلاه).
- `_validate_stored_sheets` تُستدعى فقط من `_stored_compute_response`، والتي بدورها لا تُستدعى إلا عندما تكون `state.stage` تساوي `"computed"` أو `"confirmed"` (حراسة موجودة بالفعل في `get_nesting_job`).
- `delete_job_part` تُعيد ضبط `state.stage` إلى `"uploaded"` في نفس الكتلة — فيصبح الكاش القديم بيانات ميتة غير قابلة للوصول أصلاً، حتى قبل أي إصلاح.

### الحل المطبَّق

تم إضافة إعادة ضبط الحقول الأربعة الناقصة في نفس الموضع، للاتساق مع النمط المُتَّبع بالفعل في المشروع (دفاع في العمق — "unreachable but checked anyway"، وهي فلسفة موثَّقة صراحة في مكان آخر من نفس الملف عبر `_reports_from_cached_violations`).

---

---

## ISSUE-10: مؤشر الاتصال بالباكإند يفضل عالقاً على "غير متصل" مع أن الباكإند شغال فعلاً

**الملف:** `web/src/app/page.tsx`
**الخطورة:** متوسطة — تجربة مستخدم مربكة جداً ("البرنامج بيقول غير متصل رغم إن السيرفر شغال")، لكن لا أثر على صحة البيانات أو منطق الـnesting.
تم اكتشافها **بعد** كتابة التقرير الأصلي لهذه الجلسة، بناءً على بلاغ مباشر من صاحب المشروع.

### المشكلة كما اكتُشفت

الفرونت إند (`web/src/app/page.tsx`) كان يستدعي `initialize()` مرة واحدة فقط عند تحميل الصفحة (`useEffect` بمصفوفة اعتماديات `[]`)، وهذه بدورها تستدعي `checkServer()` مرة واحدة فقط — والتي بدورها تستدعي `api.healthCheck()` (`GET /health`) مرة واحدة فقط، وتحفظ النتيجة في `serverReachable` (`lib/nestingJobStore.ts`، السطر 225-232).

**لا يوجد أي آلية polling دورية تعيد الفحص من تلقاء نفسها بعد ذلك.**

السيناريو المؤكَّد الذي أبلغ عنه صاحب المشروع:

1. فتح صفحة الفرونت إند (`http://localhost:3000`) قبل ما الباكإند يكون جاهزاً أو أثناء بدء تشغيله.
2. `checkServer()` يتنفذ مرة واحدة، يفشل (السيرفر لم يرد بعد)، فيُحفَظ `serverReachable = false`.
3. يُشغّل صاحب المشروع الباكإند بعد ذلك مباشرة، ويصبح `GET /health` يرد 200 بشكل مثالي من هذه اللحظة.
4. لكن الواجهة ما كانت هتعيد السؤال تلقائياً أبداً — فالنقطة الحمرا تفضل واقفة على "غير متصل" لحد ما يُجري فعل يدوي (ضغط "حفظ والاتصال محلياً" أو إعادة تحميل الصفحة كاملة).

### التأكيد التجريبي

- تم التحقق من أن الباكإند شغال فعلاً وقت المشكلة: `curl` مباشر لـ`http://127.0.0.1:8000/health` رجّع `{"status":"ok"}` بـ`HTTP 200` في `0.0027s`.
- تم التحقق من أن رابط ngrok المعروض في الصورة يرد 200 مع هيدر `access-control-allow-origin: *` صحيح أيضاً — ليس مشكلة CORS ولا mixed-content (الصفحة كانت `http://localhost:3000`، لا يوجد تعارض بروتوكول مع الرابط المحلي).
- تم التحقق عبر قراءة مباشرة لـ`app/page.tsx`، `lib/nestingJobStore.ts`، و`lib/nestingApi.ts` أن منطق الفحص والمعالجة سليم بالكامل، وأن الثغرة الوحيدة هي غياب أي آلية إعادة فحص تلقائية.

### الحل المطبَّق

تمت إضافة `useEffect` جديدة في `app/page.tsx` تقوم بـ:

1. إعادة استدعاء `checkServer()` كل 5 ثوانٍ طالما التاب ظاهر (`document.visibilityState === "visible"`)، لتجنُّب طلبات شبكية غير ضرورية لتاب في الخلفية.
2. إعادة الفحص فوراً عند رجوع التاب للfocus أو الظهور (`visibilitychange` و`focus` events)، وهي بالضبط اللحظة الأكثر احتمالاً لتشغيل الباكإند ثم الرجوع للفرونت إند.

تكلفة الفحص الدوري منخفضة عمداً: `healthCheck()` طلب `GET /health` واحد بمهلة 3 ثوانٍ (راجع `nestingApi.ts`)، فالتكرار كل 5 ثوانٍ لا يشكّل أي ضغط ملحوظ على الباكإند أو المتصفح.

### التحقق بعد الإصلاح

- TypeScript: نجح بلا أخطاء.
- ESLint: نجح بلا أخطاء.
- الملف أُعيدت قراءته بعد الكتابة للتأكد من أن المحتوى المكتوب مطابق تماماً للمطلوب، وأن الـ`useEffect` الآخر المسؤول عن التنقل بين الشاشات (upload/preview/export) لم يتأثر.

---

# القسم 3: توصية موثَّقة (لم تُصلَح بعد)

## ISSUE-9: `ConfirmResponse` لا يُرسل حقول الفحص الحقيقية من `QaReport`

**الملفات:** `backend/app/validation/qa_check.py`, `backend/app/api/schemas.py`, `backend/app/main.py`, `web/src/lib/nestingJobStore.ts`, `web/src/components/ExportScreen.tsx`
**الخطورة:** منخفضة — **لا يوجد أي أثر حالي على تجربة المستخدم**، لكنه "فخ" كامن لأي تطوير مستقبلي.

### المشكلة

`qa_check.py`'s `run_qa_check()` يُنتج `QaReport` يحتوي على خمسة حقول تفصيلية:
`checked_dimension`, `checked_dpi`, `checked_clearance_pairs`, `checked_icc_and_mode`, `checked_layers` — وهذه بالضبط الحقول التي يعتمد عليها `QaReport.is_valid` نفسه.

لكن `main.py`'s بناء `ConfirmResponse` (نهاية `confirm_and_export`) **لا يُمرِّر أياً من هذه الحقول الخمسة على الإطلاق** — فقط `qa_report.is_valid` (كـ`export_accepted`) و`qa_report.violations`. `schemas.py`'s `ConfirmResponse` لا يملك حقولاً لها من الأساس.

### الأثر الفعلي (تم التحقق منه بدقة)

الفرونت إند (`types/nestingJob.ts`'s `QaReport`) يُعرِّف هذه الحقول الخمسة كأنها حقيقية قادمة من الخادم، ويستهلكها `applyExportData` في `nestingJobStore.ts`:

- **أربعة من الخمسة** (`checkedDimension`, `checkedDpi`, `checkedIccAndMode`, `checkedLayers`) تُعاد بناؤها كـ`!violations.some(v => v.severity === X)`. تم التحقق بقراءة كل دالة فحص في `qa_check.py`: كل حالة تُرجع فيها الدالة `False` تُصاحبها دائماً إضافة مخالفة بنفس الـseverity (باستثناء حالة `FILE_UNREADABLE`، والتي تظهر عبر القائمة العامة للمخالفات على أي حال). **لذلك هذا الجزء يعمل بشكل صحيح فعلياً اليوم، رغم أنه إعادة بناء غير ضرورية لمنطق موجود بالفعل بشكل صحيح في الخادم.**
- **الخامس، `checkedClearancePairs`،** لا يملك أي بديل مبني على المخالفات (لأنه رقم كمي وليس pass/fail)، فيلجأ الفرونت إند لحساب **قيمة مُخترَعة**: `placedCount * (placedCount - 1) / 2` — أي العدد التوافقي الكامل لكل الأزواج الممكنة، **وليس** العدد الحقيقي الذي فحصه `qa_check.py` فعلياً عبر تصفية STRtree المكانية (وهو عادة أقل بكثير).

**تم التحقق عبر grep أن هذه القيمة المُخترَعة غير معروضة في أي مكان من الواجهة حالياً** (`ExportScreen.tsx`'s صف "Clearance" يستخدم فحص وجود مخالفة، وليس الرقم نفسه). **لذلك لا يوجد أي خطأ ظاهر للمستخدم اليوم.**

### لماذا هذا لا يزال يستحق التوثيق

هذا الحقل "قنبلة موقوتة صامتة": لو أضاف أي تطوير مستقبلي عرضاً لهذا الرقم في الواجهة متوقعاً القيمة الحقيقية، سيعرض قيمة خاطئة بصمت دون أي إشارة للخطأ — لأن عقد الـAPI الحالي ببساطة لا يحمل الرقم الحقيقي أصلاً.

### التوصية (لم تُطبَّق في هذه الجلسة، بقرار متعمَّد)

لم يتم تعديل `schemas.py` أو `main.py` أو `nestingJobStore.ts` لهذا السبب:

1. لا يوجد خلل حي يستدعي إصلاحاً عاجلاً.
2. توسيع عقد الـAPI (`ConfirmResponse`) قرار هندسي يستحق أن يُتَّخذ بوعي، لا أن يُدمج ضمنياً في مهمة تحليل.

**الحل المقترح عند الرغبة في معالجته:**

1. إضافة الحقول الخمسة إلى `schemas.py`'s `ConfirmResponse`.
2. تمريرها من `qa_report` في `main.py`'s بناء الاستجابة النهائية (السطر ~1660-1680).
3. تحديث `nestingJobStore.ts`'s `applyExportData` لقراءتها مباشرة من الاستجابة بدل إعادة بنائها من المخالفات.

هذا تغيير بسيط النطاق (3 ملفات، تعديل إضافي وليس هدماً) عند اتخاذ قرار تنفيذه.

---

# القسم 4: ملاحظات كود ميت (لا خطورة، موثَّقة للاكتمال فقط)

هاتان الحالتان **ليستا أخطاء** — كود عام غير مستخدم، تم توثيقهما فقط لأن منهجية هذا التحليل تتطلب الشمولية الكاملة.

1. **`backend/app/rasterization/tiff_export.py`'s `export_tiff()`** (النسخة أحادية الورقة): لا يوجد أي استدعاء لها في المشروع بالكامل (تم التحقق بـgrep). المسار الوحيد المُستخدَم فعلياً من `main.py`'s `confirm_and_export` هو `export_multi_sheet_tiff`، والذي يُغلق كل `composited.layers` والـcanvas بشكل صحيح في `finally`. لا يوجد أي تسريب موارد لأن الدالة غير المستخدمة لا تُستدعى أصلاً.

2. **`web/src/lib/nestingApi.ts`'s `exportCurrentLayout()`**: لا يوجد أي استدعاء لها في `web/src` بالكامل، **وendpoint المستهدف `POST /layout/export/{job_id}` غير موجود في `main.py` من الأساس** (تم التحقق بـgrep، صفر تطابق). لو استُدعيت هذه الدالة يوماً، ستفشل بـ404 — لكن بما أن لا شيء يستدعيها، فلا يوجد خطر حي.

---

## ملخص الحالة النهائية

- **9 من 9** مشاكل مؤكَّدة من التحليل الأولي (ISSUE-1 إلى ISSUE-8) **تم تنفيذها والتحقق من وجودها في الكود الحالي بالقراءة المباشرة**، وليس بالاعتماد على تقرير أو ذاكرة مهمة سابقة فقط.
- **ISSUE-10** اكتُشفت وأُصلحت في جلسة منفصلة بعد بلاغ مباشر من صاحب المشروع: مؤشر الاتصال في شاشة الإعدادات كان يفضل عالقاً على "غير متصل" بسبب فحص أحادي عند تحميل الصفحة بلا أي polling دوري، تم إصلاحه بإضافة فحص دوري كل 5 ثوانٍ + فحص فوري عند رجوع التاب لل focus.
- **ISSUE-7 (الأخطر في كامل التحليل عبر الجلستين)** كانت عطلاً حرجاً حقيقياً قابلاً للتكرار فعلياً (وليس نظرياً)، تم تأكيده تجريبياً قبل وبعد الإصلاح، وتم التحقق من عدم وجود أي نظير آخر له عبر grep شامل لكل نقاط استدعاء الدالتين المعنيتين في المشروع بالكامل.
- **87 اختبار pytest** (الحزمة الكاملة الموجودة في `backend/tests/`) نجحوا جميعاً (0 فشل) بعد إصلاح ISSUE-7، مؤكِّدين عدم وجود أي regression.
- **ISSUE-9** توصية موثَّقة بوضوح مع حل مقترح جاهز، لم تُطبَّق بقرار متعمَّد لأنها لا تُشكِّل خللاً حياً حالياً.
- تم التحقق من مقاومة `compositor.py`'s رياضيات الدوران للتحقيق الدقيق (ثلاث طرق مستقلة، بما فيها اختبار end-to-end بسلسلة الاستدعاء الإنتاجية الفعلية) — **لا يوجد أي خلل هناك**، رغم أن اختباراً أولياً أقل دقة أوحى بوجود مشكلة (تم توثيق هذا صراحة لمنع إعادة التحقيق فيه مستقبلاً).
- لم يُعثر على أي مشكلة أمان أطفال (child safety)، ولا أي مشكلة تصميم بنيوي خطير، خلال هذا التحليل الكامل عبر الجلستين.
- **جميع ملفات `backend/app/**` (27 ملف) وجميع ملفات `web/src/**` (20 ملف) قُرئت بالكامل، سطراً بسطر، بلا استثناء واحد.**
